# Processed Annotations

This directory contains generated pathway membership, pathway edge, and
miRNA-target tables. These files are ignored by Git and can be rebuilt from the
raw resources with `scripts/construct_pathways.py`.
